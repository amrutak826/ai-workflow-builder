# Copy to .env (nhost CLI loads this automatically with `nhost up`)
ACTION_SECRET=change-me-to-a-long-random-string
HASURA_GRAPHQL_ADMIN_SECRET=change-me-too
LLM_PROVIDER=stub          # stub | groq | openrouter | gemini
LLM_API_KEY=
SLACK_WEBHOOK_URL=         # leave blank to use the stubbed notify path
